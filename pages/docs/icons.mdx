import DocsLayout from "../../components/layouts/DocsLayout.js";
import { SearchIcons } from "../../components/Icon.js";

## Available Icons

We use the libraty `React Icons` that includes most icon sets, so you have many to chose from.

_NOTE: We use the latest version of each icon set, so where some icon sets have multiple versions, we only use the latest version. For example `Heroicons` and `Heroicons 2` - we use `Heroicons 2`_

Search for your icons here https://react-icons.github.io/react-icons at the top left of the page.

export default ({ children }) => <DocsLayout>{children}</DocsLayout>;
