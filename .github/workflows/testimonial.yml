name: Testimonial creation
  
on:
  issues:
    types: [opened]

jobs:
  add_testimonial:
    runs-on: ubuntu-latest
    name: Required Label
    steps:
      - uses: renanmav/match-label-action@v3
        with:
          allowed: >
            testimonial
      - name: Deploy blog
        if: steps.label.outputs.match == 'testimonial'
        run: echo "Adding testimonial"