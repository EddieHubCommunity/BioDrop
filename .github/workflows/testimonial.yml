name: Testimonial creation
  
on:
  issues:
    types: [opened]


jobs:
  add_testimonial:
   if: ${{ github.event.label.name == 'testimonial' }}
   name: 'Add Testimonial file'
   runs-on: ubuntu-latest
   steps:
      - name: Check out code
        uses: actions/checkout@v3